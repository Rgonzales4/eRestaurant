<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('partials/navbar') %>
    <title>Create Booking</title>
  </head>

  <body>
    <div class="container">
      <div class="row">
        <div class="col-3">
         <h1 class="mb-2">Create Booking</h1>
      <!-- <% if(req.user){%>
    <div>
      <label
      for="bookingUser"
      name="bookingUser"
      value="<%- req.user.email%>">
    </label>
    </div>
    <%}   %> -->
      <div>
        <span style="color: green"><%= successMessage %></span>
        <span style="color: red"><%= failMessage %></span>
      </div>

      <form action="/bookings" method="POST" style="margin: 20px">
        <%- include('partials/_booking_form_fields') %>
      </form>
    </div>

    <div class="col-4">
      <h1>Menu</h1>
      <% Array.from(menu).forEach(item => {%>
        <div class="input-group mb-1">
          <input
            disabled="true "
            type="text"
            class="form-control"
            value="<%= item.itemName %>"
          />
        </div>
        <% })%>
    </div>

    <div class="col-sm">
      <h1>Cart</h1>
      <p style="border:3px; border-style:solid; border-color:#b9b9b9; padding: 1em;">Please <strong>EDIT</strong> Your Booking After <strong>SAVING</strong> To<br><strong>ADD MENU ITEMS</strong> To Your Cart.</p>
      <h2></h2>
    </div>

    </div>
  </body>
</html>
